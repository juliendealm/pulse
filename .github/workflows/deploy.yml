name: Deploy to Vercel

# Déploiement manuel depuis le terminal : npx vercel --prod
# Ce workflow est désactivé — le deploy Vercel Hobby + repo privé
# ne supporte pas le deploy automatique depuis GitHub Actions de manière fiable.

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy manually with: npx vercel --prod"
